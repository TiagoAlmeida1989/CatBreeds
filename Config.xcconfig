// Config.xcconfig
// Non-secret build configuration. Committed to source control.
// Override these values per build configuration (Debug/Release/Staging)
// by creating separate xcconfig files that include this one.

CAT_API_BASE_URL = https://api.thecatapi.com/v1
